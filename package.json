{
  "name": "chat-widget-sdk",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build && node open-dev.js",
    "preview": "vite preview",
    "deploy": "vite build && npm version patch && git tag v$(node -p \"require('./package.json').version\") && git push origin v$(node -p \"require('./package.json').version\")"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.5",
    "tailwindcss": "^4.1.5",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@types/node": "^22.15.16",
    "@vitejs/plugin-vue": "^5.2.3",
    "@vue/tsconfig": "^0.7.0",
    "typescript": "~5.8.3",
    "vite": "^6.3.5",
    "vite-plugin-css-injected-by-js": "^3.5.2",
    "vue-tsc": "^2.2.8"
  }
}
